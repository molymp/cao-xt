# Habacher Dorfladen Kiosk – Python-Abhängigkeiten
# Installation: pip3 install -r requirements.txt --break-system-packages

flask>=3.0
mysql-connector-python>=8.0
waitress>=3.0           # Prod-WSGI-Server (ersetzt Werkzeug-Dev-Server)
gspread>=6.0
google-auth>=2.0

# python-escpos wurde entfernt – Bondruck läuft über direkten TCP-Socket (ESC/POS-Rohbytes)
